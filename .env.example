# GitHub OAuth — https://github.com/settings/developers
AUTH_SECRET="generate with: openssl rand -base64 32"
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# PostgreSQL for production (leave empty for SQLite local dev)
DATABASE_URL=

# Code execution runner (optional, uses local subprocess if unset)
RUNNER_URL="http://localhost:8080"
