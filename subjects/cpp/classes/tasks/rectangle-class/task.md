# Класс Rectangle

Реализуйте класс `Rectangle` с полями `width` и `height`, конструктором с двумя параметрами, и методом `int area()`.

Программа считывает два числа и выводит площадь прямоугольника.

## Пример

Ввод: 4 5 → Вывод: 20
