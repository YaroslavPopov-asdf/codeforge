#include <iostream>
using namespace std;

// Напишите функцию sumArray через указатели здесь

int main() {
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    cout << sumArray(arr, n) << endl;
    return 0;
}
