# Наибольший общий делитель

Напишите рекурсивную функцию `int gcd(int a, int b)`, которая вычисляет НОД по алгоритму Евклида:

- Если b = 0, то НОД(a, b) = a
- Иначе НОД(a, b) = НОД(b, a % b)

## Пример

Ввод: 12 8 → Вывод: 4
