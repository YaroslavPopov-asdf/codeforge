# Реверс строки

Напишите программу, которая считывает строку и выводит её в обратном порядке.

## Требования

1. Строка подаётся на вход (одна строка)
2. Вывести перевёрнутую строку
3. Максимальная длина строки — 100 символов

## Пример

```
Ввод: hello
Вывод: olleh
```
