#include <stdio.h>

// Напишите функцию sumArray через указатели здесь

int main() {
    int n;
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    printf("%d\n", sumArray(arr, n));
    return 0;
}
