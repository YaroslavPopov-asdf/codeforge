# Калькулятор

Создайте перечисление `Operation` (Add, Subtract, Multiply, Divide) и функцию `fn calculate(a: f64, op: Operation, b: f64) -> f64`.

Программа считывает: число a, оператор (+, -, *, /), число b. Используйте match.

## Пример

Ввод: 10 + 5 → Вывод: 15
