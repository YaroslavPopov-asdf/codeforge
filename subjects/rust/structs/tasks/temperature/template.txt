use std::io;

// Напишите структуру Temperature и её метод здесь

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    let celsius: f64 = input.trim().parse().unwrap();
    let t = Temperature { celsius };
    println!("{}", t.to_fahrenheit());
}
